<% if @answer.valid? %>
  $('.answers').html("<%= j render 'questions/answers' %>");
  $('#answer_body').val('');
  $('#errors').empty();
<% else %>
  $('.answer_form').html("<%= j render 'answers/create_form' %>")
<% end %>